<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".ui.main.DanhSachGiaoDichActivity">

    <!-- 1. Đặt Toolbar ở trên cùng của Activity -->
    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="?attr/colorPrimary"
        app:title="Tất Cả Giao Dịch"
        app:titleTextColor="@android:color/white"
        app:navigationIcon="@drawable/ic_home_24" />

    <!-- 2. RecyclerView để hiển thị danh sách các item nằm bên dưới -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_view_all_transactions"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
        tools:listitem="@layout/item_giao_dich" />

</LinearLayout>
